Тестовое задание

API приложения для ведения личного бюджета

**ENDPOINTS**

`/api/wallets` POST

`/api/wallets` GET

`/api/wallets/<pk>` GET

`/api/wallets/<pk>` PUT

`/api/wallets/<pk>` DELETE

`/api/operations` GET

`/api/operations` POST

`/api/operations/<pk>` DELETE